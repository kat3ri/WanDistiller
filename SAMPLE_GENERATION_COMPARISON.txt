================================================================================
SAMPLE GENERATION: BEFORE vs AFTER PARALLELIZATION
================================================================================

SCENARIO: 4 GPUs, 4 Sample Prompts, ~2.5 min per sample

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

BEFORE (Sequential on Rank 0 only):
────────────────────────────────────────────────────────────────────────────

Time:  0m    2.5m    5m     7.5m    10m
       │      │      │       │       │
GPU 0: [████][████][████][████]     ← Generates all 4 samples (10 min)
GPU 1: [░░░░░░░░░░░░░░░░░░░░]       ← Waiting at barrier (idle)
GPU 2: [░░░░░░░░░░░░░░░░░░░░]       ← Waiting at barrier (idle)
GPU 3: [░░░░░░░░░░░░░░░░░░░░]       ← Waiting at barrier (idle)

Total Time: 10 minutes
GPU Utilization: 25% (only 1 of 4 GPUs working)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

AFTER (Parallel across All GPUs):
────────────────────────────────────────────────────────────────────────────

Time:  0m    2.5m
       │      │
GPU 0: [████]        ← Sample 1: "A cat"
GPU 1: [████]        ← Sample 2: "A dog"
GPU 2: [████]        ← Sample 3: "A bird"
GPU 3: [████]        ← Sample 4: "A fish"
       └──────┘
    All GPUs sync at barrier

Total Time: 2.5 minutes
GPU Utilization: 100% (all 4 GPUs working)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

IMPROVEMENT:
────────────────────────────────────────────────────────────────────────────

⚡ Speedup:        4.0x faster (10 min → 2.5 min)
📊 GPU Usage:      4x more efficient (25% → 100%)
🎯 Scalability:    Automatically uses all available GPUs
✅ Compatibility:  No command-line changes needed
🔒 Thread-Safe:    Rank-specific filenames prevent conflicts

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

KEY INSIGHT:

With N GPUs, you get approximately Nx speedup:
  • 2 GPUs: 2x faster
  • 4 GPUs: 4x faster  
  • 8 GPUs: 8x faster

This is achieved by distributing the sample prompts across all available GPUs,
so each GPU only needs to generate 1/N of the total samples.

================================================================================
